% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-experiments-over-networks.R
\name{estimator_AS_tidy}
\alias{estimator_AS_tidy}
\title{Tidy helper function for estimator_AS function}
\usage{
estimator_AS_tidy(data, p_matrix, adj_matrix, obs_prob_exposure)
}
\arguments{
\item{data}{A \code{data.frame} containing the observed data.}

\item{adj_matrix}{An adjacency matrix defining the network structure. This
can be created, for example, as follows:
\preformatted{
  adjacency <- fairfax |>
    as("Spatial") |>
    spdep::poly2nb(queen = TRUE) |>
    spdep::nb2mat(style = "B", zero.policy = TRUE)
  }}

\item{obs_prob_exposure}{A set of exposure probabilities. These can be
generated, for example, using:
\preformatted{
  prob_exposure <- interference::make_exposure_prob(
    potential_tr_vector = permutations,
    adj_matrix = adjacency,
    exposure_map_fn = interference::make_exposure_map_AS,
    exposure_map_fn_add_args = list(hop = 1)
  )
  }}

\item{permutation_matrix}{A matrix of random treatment assignments. Each row
corresponds to one permutation of the treatment vector.}
}
\value{
a data.frame of estimates
}
\description{
Runs estimates estimation function from interference package and returns tidy data frame output
}
\details{
The estimator_AS_tidy function requires the 'interference' package, which is not yet available on CRAN.

To use this function:
\enumerate{
\item install the developer version of interference via remotes::install_github('szonszein/interference') and
\item install the developer version of rdss via remotes::install_github('DeclareDesign/rdss@remotes')
}

See https://book.declaredesign.org/experimental-causal.html#experiments-over-networks
}
